# Monotonic Stack in Python
=====================================

A monotonic stack is a stack data structure that always maintains a monotonic order on its elements. The order is either increasing (non-decreasing) or decreasing (non-increasing).

We can implement a monotonic stack using a regular stack and modifying the push and pop operations to maintain the monotonic order.

### Monotonic Stack Implementation